# dependencies (Railway runs `npm install`)
node_modules/

# runtime multiplayer session logs (ephemeral)
logs/
*.log

# scratch / throwaway scripts and dumps
_*.mjs
_*.out
*.b64

# unused art masters / packs moved aside (kept locally, not shipped)
_unused_assets/

# os
.DS_Store
Thumbs.db
